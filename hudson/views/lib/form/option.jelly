<!--
	<option> tag that takes true/false for selected
-->
<j:jelly xmlns:j="jelly:core" xmlns:d="jelly:define">
	<j:choose>
		<j:when test="${attrs.selected}">
			<option value="${attrs.value}" selected="true"><d:invokeBody/></option>
		</j:when>
		<j:otherwise>
			<option value="${attrs.value}"><d:invokeBody/></option>
		</j:otherwise>
	</j:choose>
</j:jelly>